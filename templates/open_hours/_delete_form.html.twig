<form method="post" action="{{ path('app_open_hours_delete', {'id': open_hour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ open_hour.id) }}">
    <button class="btn">Delete</button>
</form>
